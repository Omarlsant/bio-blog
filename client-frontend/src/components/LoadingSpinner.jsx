import React from 'react';

const LoadingSpinner = () => (
    <div className="flex justify-center items-center min-h-screen">
        <p className="text-green-600 font-semibold">Cargando contenido...</p>
    </div>
);

export default LoadingSpinner;
